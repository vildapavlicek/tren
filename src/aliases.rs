use rust_decimal::Decimal;

/// These are just helper aliases for types to make it easier for reading
/// when using HashMaps

pub type ClientID = u16;
pub type TransactionID = u32;
pub type Amount = Decimal;
